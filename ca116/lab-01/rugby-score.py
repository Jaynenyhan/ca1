#!/usr/bin/env python

# Assume existing integer variables tries, conversions and penalties
# Try -> 5 pts
# Conversion -> 2 pts
# Penalty -> 3 pts

score = (5 * tries) + (2 * conversions) + (3 * penalties)
