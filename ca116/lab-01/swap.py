#!/usr/bin/env python

# Assume existing integer variables n and m.
# Swap values in n and m

t = m     # Put m in temp variable t
m = n     # Overwrite m with n
n = t     # Overwite n with what was m, from t
